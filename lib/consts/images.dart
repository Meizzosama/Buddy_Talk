import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

const String logo = "assets/icons/ic_logo.png";
const ic_user = "assets/icons/ic_user.png";

const settingsIcon = Icons.settings;

const invites = "assets/icons/invite.png";

const accounts = Icon(Icons.person);
const friends = Icon(Icons.people);
const noti = Icon(Icons.notification_important);
const data = Icon(Icons.data_usage);
const help = Icon(Icons.help);

const drawerIconsList = [accounts, friends, noti, data, help];
